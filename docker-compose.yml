version: "3.9"

services:

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sixtech
      POSTGRES_PASSWORD: sixtech123
      POSTGRES_DB: sixtech

  redis:
    image: redis:7

  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "3000:8080"
